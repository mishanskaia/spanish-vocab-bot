# TODO — бэклог spanish_vocab_bot

Рабочий список задач с приоритетами. Обновляется по ходу дела — отмечай `[x]` по мере выполнения,
добавляй новое снизу нужной секции. Приоритет — не жёсткий порядок выполнения, а ориентир для
следующего шага, когда есть 5-10ч/неделю на проект (см. project-reading-site-combo в памяти).

## P0 — сейчас

### Раздача на 5 знакомых — три открытых вопроса в проработке (сессия `/grill-me` 2026-09-12)

- [ ] **1) Онбординг-файл.** PDF с описанием бота для тестировщиков — вместо того чтобы выводить лимит
  (35 слов/неделю, сброс по понедельникам — было 5/день до 2026-09-13) и прочие условия теста прямо в
  `/start`, решено сделать отдельный PDF, который раздаётся вместе с инвайт-ссылкой. Не начато.
- [ ] **2) Как собрать фидбек + как кончается использование.** Два переплетённых открытых вопроса,
  сознательно отложены на подумать, не решены:
  - *Фидбек-канал* — сейчас у бота нет способа собрать обратную связь от тестировщика (ни команды
    `/feedback`, ни любого другого канала внутри бота) — либо человек пишет тебе отдельно от бота,
    либо просто молчит. Решить: отдельный канал вне бота (созвон/чат) или что-то простое в самом
    боте (например, `/feedback <текст>`, пересылающая сообщение тебе с именем отправителя)?
  - *Отзыв доступа + продолжительность теста* — сейчас `db.is_user_authorized()` авторизует
    навсегда после погашения инвайт-кода, отозвать нельзя вообще (только руками лезть в БД). Нужно
    решить и внедрить: (а) сам механизм — ручная команда `/revoke <user_id>` (простое MVP, по твоему
    решению) или автоматический срок доступа, зафиксированный при выдаче инвайта (аналогично X+30 у
    Study Coach — дата не пересчитывается); (б) саму продолжительность теста (2/3/4 недели?);
    (в) заодно, если будет `/revoke` — видимость по тестовой группе (кто воспользовался инвайтом,
    когда, насколько активен), чтобы было на основе чего решать, кого отзывать. Данные при отзыве
    **не удалять** (решено в сессии `/grill-me`) — только блокировать вход через `access_gate`,
    словарь остаётся на случай, если человека вернут в тест.
- [x] **3) Аудит на баги перед раздачей** — сделано 2026-09-13 (финальная проверка перед первой реальной
  раздачей). Прогнан полный логический тест (`logic_test.py`, реальные `db.py`/`bot.py`, изолированная
  временная БД, без реального вызова Claude) — 37 проверок: инвайт-гейт, часовой пояс, дедуп до Claude,
  недельный лимит и его сброс по понедельникам, генерация карточек повторения, лимит и антифишинг
  мнемоник, бэкап, IDOR-защита на чужие word_id. Найдено и поправлено две реальные проблемы: (1)
  сообщение о лимите сессии внутри `/review` всё ещё показывало захардкоженное "10:00/14:00/18:00 МСК"
  вместо "по твоему времени"; (2) в вопросе про время при `/start` не было выхода — если человек
  игнорировал вопрос и пробовал сразу прислать слово, бот вечно отвечал "не поняла, напиши время
  цифрами" на любое сообщение без способа выбраться — добавлены слова-эскейпы ("позже"/"пропустить").
  См. CLAUDE.md «Часовой пояс пользователя». **Не проверено** — реальный диалог в Telegram живым
  человеком (кнопки, порядок сообщений); логический тест этого не покрывает.
- [ ] **Продуктивное владение → «Вечерняя практика» голосом (owner-only).** Решено 2026-09-15 после
  обсуждения + `/llm-council`. Совет рекомендовал текст внутри сессии карточек, но это опровергнуто
  реальным сценарием Ольги: карточки листаются на работе урывками и молча, а составлять и
  проговаривать фразы удобно только вечером дома (и она уже делает это сама по словам из слота 2
  Study Coach). Поэтому практика отделена от повторения по времени. Голосовые сообщения, а не
  real-time голос — решение 2026-08-30 «не конкурировать с real-time voice-AI» не нарушается.
  Спека:
  - 21:00 по времени владельца — сообщение «Пора поговорить» с теми же 3 словами, что утром дал
    Study Coach; слова показываются **по-русски**, испанское — под спойлером (активное вспоминание).
  - На каждое слово: голосовое с фразой → бот показывает «🎧 Услышал: <транскрипт>» (чтобы было видно,
    не «причесал» ли STT ошибку) → одна правка + одна строка пояснения текстом (голос от бота не нужен)
    → повторная запись с учётом правки → «супер, дальше».
  - Максимум 3 попытки на слово + всегда кнопка «Дальше»; после 3-й — переход без блокировки.
  - Слишком простая фраза (3–4 слова) — засчитывается, но бот мягко предлагает усложнить; жёсткого
    порога по числу слов нет.
  - Голосовые сейчас боту не нужны ни для чего другого → не конфликтуют с добавлением слов текстом,
    флаг-ловушка вида `awaiting_*` не нужен. Текстовый ответ (если будет) — только reply на задание.
  - Стек: OpenAI (STT) + Claude (разбор). Нужен `OPENAI_API_KEY` в Railway.
  - Для других пользователей — позже и с текстовым ответом наравне с голосом (для A1 голосовое —
    барьер). У тестеров пока нет слов stage ≥3.
  Шаги: (1) Ольга заводит ключ OpenAI API; (2) тест STT на её голосе — 4–5 голосовых с намеренными
  ошибками, невнятным словом, русским словом в середине — проверить, что сохраняется в транскрипте;
  (3) план реализации.
  **Результат шага 2 (2026-09-15, `tests/stt_check.py`, 5 записей):** `gpt-4o-mini-transcribe` + промпт
  «дословно» сохранил все намеренные ошибки: *yo fue*, *el casa* и русское слово посреди фразы.
  `whisper-1` не подходит: исправил *fue*→*fui* и перевёл русское слово. Без промпта модели gpt-4o
  тоже переводили русское слово на испанский, так что промпт обязателен. Невнятное слово модели
  gpt-4o угадывают по контексту, поэтому как проверка произношения STT не годится. Старый текстовый черновик в stash (`speaking-task feature`) — неактуален,
  брать из него максимум промпт `check_sentence`.

## P1 — следующее

- [ ] **Структура множественной кодировки** (что/как/когда: текст-ассоциация, картинка, может звук) —
  сначала продумать структуру целиком, не начинать с картинки в одиночку. Это ядро дифференциации:
  research показал, что "слова не запоминаются через изолированную зубрёжку" — самая частая жалоба
  на конкурентов, мнемоники-в-контексте — прямое попадание в эту боль.
- [x] **Промт текстовой ассоциации работает плохо** — сделано 2026-09-03. Новый промпт в
  `ai_helper.get_mnemonic`: слово разбивается на звуковые куски, покрывающие его целиком от начала до
  конца (не только удобный кусок в середине/хвосте), куски сшиваются в одну сценку, где один образ
  физически перетекает в другой (метод keyword/linkword), плюс fallback на образ по смыслу для
  слов без внятного созвучия (короткие/служебные). Модели также явно запрещено протаскивать в ответ
  черновые рассуждения о неудачных попытках созвучия. Протестировано вручную на нескольких словах.
- [x] **Английские слова маскировались под русское созвучие** — сделано 2026-09-06. Модель иногда
  выдавала транслитерацию иностранного (обычно английского) слова так, будто это случайное русское
  созвучие — например "nun" для re-NUN-ciar без пометки, что это не русское слово. Промпт теперь
  требует: 1) созвучие должно быть реальным русским словом/выражением; 2) английское разрешено, но
  только если оно отчётливо сильнее русского, и ВСЕГДА с явной пометкой языка и переводом —
  `"nun" (англ. "монашка")`; 3) без обоих — fallback на образ по смыслу (ШАГ 1-Б), как и раньше.
- [ ] **Картинка для мнемоник** — как лучше сделать. Делать после/вместе со структурой выше, не раньше.
- [x] **Поменять multiple choice** — сделано 2026-09-02. Убрали MC-карточку (пропуск + 3 кнопки-варианта)
  полностью: узнавание среди вариантов слабее свободного воспроизведения (testing effect). Теперь единый
  формат — активное воспоминание с контекстом, всегда self-graded, три уровня fallback: пропуск в
  предложении → русский перевод предложения как контекст → голый перевод. Ответ всегда в словарной форме
  (диклеймер об этом показывается один раз в начале /review и /all). См. CLAUDE.md «Типы карточек при
  повторении».
- [x] **Кнопка "почти помню"** — сделано 2026-09-02. Третья градация `'almost'` (🟡) между Помню и
  Сложно: `stage` не продвигается, интервал = `INTERVALS[stage] * 0.8` (между `*1.0` у remember и `*0.6`
  у hard). Специально НЕ продвигает stage, в отличие от старого убранного `'easy'`, который сидел НАД
  remember и как раз разгонял stage слишком быстро — это и была причина его убрать. Мнемоника теперь
  предлагается на `'almost'` и `'hard'` (раньше — только на `'hard'`). См. CLAUDE.md «Логика
  интервальных повторений».
- [x] **Пересмотреть логику повторений, чтобы всё не сваливалось в утреннюю сессию** — сделано
  2026-09-03. Мини-исследование (spacing effect, Anki/SuperMemo best practices) подтвердило: 20-30
  карточек — разумный размер сессии, несколько сессий в день лучше для запоминания, чем одна большая.
  Добавлен `SESSION_WORD_LIMIT = 30` (лимит на ЛЮБУЮ сессию, не только новые слова) и третье дневное
  уведомление в 14:00 МСК — теперь 10:00 / 14:00 / 18:00 вместо утро/вечер. Уведомление показывает
  `min(бэклог, 30)`, полный бэклог — тихой строкой, только если он больше 30. См. CLAUDE.md
  «Уведомления и лимит на сессию».
- [x] **Придержать `/words`** — снято с повестки 2026-09-06: команда `/words` удалена целиком (см.
  P2 ниже), вопрос о придерживании стал неактуален.

## P2 — потом

- [x] **Убрать /words** — сделано 2026-09-06 (без замены синком топ-3000, это по-прежнему открытый
  вопрос). Причина непосредственно в моменте: бот стали давать другим пользователям, каждое слово —
  оплаченный Claude-вызов с личного ключа, а /words дороже всего по токенам и не нужна прямо сейчас.
  Заодно закрыло известную проблему таймаута (15-30 сек на генерацию 5 слов) и риск "AI-slop". Если
  вернётся — см. идею синка со списком топ-3000 слов с избеганием по темам (открытый вопрос: что
  вообще значит "тема" в этом контексте и как это должно быть устроено в UX).
- [ ] **Персонализированная кривая забывания** — вместо фиксированных INTERVALS. R&D-задача, скорее
  всего пока мало данных о повторениях по каждому пользователю, чтобы это было осмысленно.

## P3 — на досуге

- [ ] **Возможен дубль напоминания при частых рестартах в течение часа.** Найдено при финальном
  ревью 2026-09-13. `hourly_reminder_job` запускается сразу при старте процесса (`first=0`) и потом
  раз в час — если Railway перезапускает бота несколько раз в течение одного и того же часа (типично
  во время активного деплоя, как было сегодня), и у кого-то как раз сейчас совпадает локальный слот
  10/14/18, а он ещё не повторял сегодня — реминдер уйдёт при каждом таком рестарте, не один раз.
  В обычной эксплуатации (без деплоев несколько раз в час) не проявляется. Фикс потребовал бы хранить
  "уже отправлен реминдер в этот слот сегодня" по пользователю — не делала, сочла избыточным для
  вероятности события сейчас.

- [ ] **Включить WAL-режим SQLite** (`PRAGMA journal_mode=WAL` в `db.get_connection()`) — сейчас по
  умолчанию запись блокирует файл целиком, и при одновременной записи двух пользователей второй ждёт
  до 5 сек (дефолтный busy-timeout), а не выше — не проблема. WAL снижает конфликты чтения/записи,
  дешёвая подстраховка на случай, если друзей вокруг бота станет заметно больше 2-3. Не срочно —
  всплыло при подготовке к раздаче бота 2026-09-06, риск сейчас чисто теоретический.
- [ ] **Геймификация и видимый прогресс** — делать осторожно. Research прямо подтвердил, что
  штрафующая геймификация (Duolingo hearts/streaks/ad-for-energy) — причина, по которой люди бросают,
  а не лень; текущий мягкий no-punishment дизайн бота (3 кнопки, без наказаний) — уже подтверждённое
  отличие от конкурентов, а не случайность. Если делать — только non-punishing: прогресс-бары,
  streaks без штрафов за пропуск, визуализация выученных слов и т.п.
